name: Greetings

on:
  pull_request_target:
    types: [opened, reopened]
  issues:
    types: [opened, reopened]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  greet:
    # Fork guard: issues events have no pull_request context (null check passes them
    # through); pull_request_target events from untrusted forks are blocked.
    # secrets: inherit passes all secrets to the reusable workflow.
    if: >-
      github.event.pull_request == null ||
      github.event.pull_request.head.repo.full_name == github.repository
    uses: kubestellar/infra/.github/workflows/reusable-greetings.yml@main
    secrets: inherit
