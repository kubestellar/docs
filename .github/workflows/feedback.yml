name: Feedback Wanted

on:
  pull_request:
    types: [closed]

permissions:
  contents: read
  pull-requests: write

jobs:
  feedback:
    uses: kubestellar/infra/.github/workflows/reusable-feedback.yml@a160acca0bdce1ac6c649e006d680d5f6d53024e  # main
    secrets: inherit
